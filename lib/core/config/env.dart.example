// Example Dart env holder — DO NOT store real secrets here.
// Copy this file to lib/core/config/env.dart locally (gitignored) and fill values.

class Env {
  static const baseUrl = 'https://example.com';
  static const newValue = 'CHANGE_ME';
  // Add other keys you need for local dev only
  // static const apiKey = 'CHANGE_ME';
}

